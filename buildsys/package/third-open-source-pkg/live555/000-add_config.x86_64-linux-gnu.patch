diff --git a/config.x86_64-linux-gnu b/config.x86_64-linux-gnu
new file mode 100755
index 0000000..107de49
--- /dev/null
+++ b/config.x86_64-linux-gnu
@@ -0,0 +1,19 @@
+COMPILE_OPTS 				 := $(INCLUDES) -m64  -fPIC -I. -I$(PRO_STAG_USR_PATH)/include  -O2 -DSOCKLEN_T=socklen_t -D_LARGEFILE_SOURCE=1 -D_FILE_OFFSET_BITS=64
+C 							 := c
+C_COMPILER 					 := $(CC)
+C_FLAGS 					 := $(COMPILE_OPTS)
+CPP 						 := cpp
+CPLUSPLUS_COMPILER 			 := $(CXX)
+CPLUSPLUS_FLAGS 			 := $(COMPILE_OPTS) -Wall -DBSD=1
+OBJ 						 := o
+LINK 						 := $(CXX) -o
+LINK_OPTS 					 := -L. -L$($PRO_STAG_USR_PATH)/lib
+CONSOLE_LINK_OPTS 			 := $(LINK_OPTS)
+LIBRARY_LINK 				 := ar cr 
+LIBRARY_LINK_OPTS 			 := 
+LIB_SUFFIX 					 := a
+LIBS_FOR_CONSOLE_APPLICATION := -lssl -lcrypto
+LIBS_FOR_GUI_APPLICATION 	 := 
+EXE 						 := 
+PREFIX 						 := $(CUR_PREFIX)
+LIBDIR						 := $(PREFIX)/lib
\ No newline at end of file
